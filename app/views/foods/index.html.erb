<p id="notice"><%= notice %></p>

<h1>Liste Aliments</h1>

<table class="table">
  <thead>
    <tr>
      <th>Aliment</th>
      <th>Protéines (g)</th>
      <th>Glucides (g)</th>
      <th>Lipides (g)</th>
      <th>Calories (g)</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @foods.each do |food| %>
      <tr>
        <td><%= food.name %></td>
        <td><%= food.protein %></td>
        <td><%= food.carbohydrate %></td>
        <td><%= food.lipid %></td>
        <td><%= food.calorie %></td>
        <td><%= link_to 'Afficher', food %></td>
        <td><%= link_to 'Editer', edit_food_path(food) %></td>
        <td><%= link_to 'Supprimer', food, method: :delete, data: { confirm: 'Etes-vous sur de vouloir supprimer?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nouvel aliment', new_food_path %>
